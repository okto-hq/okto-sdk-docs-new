---
title: OktoProvider
description: Learn more about the main OktoProvider here.
full: false
---

import { Tab, Tabs } from 'fumadocs-ui/components/tabs';

## Installation

The Okto SDK allows you to integrate Okto's features into your application seamlessly. Follow these steps to install the SDK using your preferred package manager.

1. Prerequisites
Ensure you have the following installed:

Node.js: Version 14 or higher.
Package Manager: npm, yarn, or pnpm.
If you don't have Node.js installed, download it from [nodejs.org](https://nodejs.org/en).

2. Install the Okto SDK
Run the following command based on the package manager you're using:

<Tabs items={['npm', 'pnpm', 'yarn']}>
        <Tab value="npm">
            ```bash
                npm i okto-sdk-react 
            ```
            </Tab>
            <Tab value="pnpm">
            ```bash
                pnpm add okto-sdk-react 
            ```
            </Tab>
            <Tab value="yarn">
             ```bash
                yarn add okto-sdk-react 
            ```
        </Tab>    
</Tabs>

This will add the Okto SDK to your project dependencies.

## Initializing the OktoProvider

The OktoProvider is the core component of the Okto SDK. It initializes the SDK and provides its functionalities to your application.

Here’s how you can set it up step by step:

Step 1: Set Up Google OAuth (Optional)
If you're using Google OAuth for authentication, initialize the GoogleOAuthProvider in your index.js file.

Open your index.js file.
Add the following code:

    ```javascript title="index.js"
        import React from 'react';
        import ReactDOM from 'react-dom/client';
        import { GoogleOAuthProvider } from '@react-oauth/google';
        import App from './App';

        const root = ReactDOM.createRoot(document.getElementById('root'));
        const GOOGLE_CLIENT_ID = "YOUR_GOOGLE_CLIENT_ID"
        root.render(
            <React.StrictMode>
                <GoogleOAuthProvider clientId = {GOOGLE_CLIENT_ID}>
                    <App />
                </GoogleOAuthProvider>
            </React.StrictMode>
        );
    ```
Why GoogleOAuthProvider?
This component manages Google OAuth for your app, allowing secure authentication via Google.

Step 2: Initialize the OktoProvider
Now, add the OktoProvider to your App.js. This component initializes the SDK and provides its context throughout your app.

Open your App.js file.
Add the following code:


   ```javascript title="App.js"
    import React from 'react';
    import { OktoProvider, BuildType } from 'okto-sdk-react';
    import LoginPage from './LoginPage';

    const OKTO_CLIENT_API_KEY = "OKTO_CLIENT_API_KEY";

    function App() {
        return (
            <OktoProvider apiKey={OKTO_CLIENT_API_KEY} buildType={BuildType.SANDBOX}>
                <LoginPage/>
            </OktoProvider>
        );
    }
    export default App;
   ```
Explanation of the Code:
OktoProvider: This component initializes the SDK and allows your app to access Okto's features.

apiKey: This is your unique API key provided by Okto. Replace "OKTO_CLIENT_API_KEY" with your actual key.

buildType: This specifies the environment type. Use BuildType.SANDBOX for testing purposes.

Tips for Beginners
Where to get API keys?

You can obtain your API key from the Okto dashboard. Contact your team lead or the Okto support team for more details.
Testing the setup:

After following the steps above, run your development server to check if everything is working.

```bash
    yarn run dev
```
    Visit http://localhost:3000 in your browser to verify the setup.

Debugging common issues:

Error: API Key Missing: Double-check that you replaced OKTO_CLIENT_API_KEY with your actual API key.
Error: Google Client ID Missing: Make sure you replaced YOUR_GOOGLE_CLIENT_ID with your Google Client ID.
   ## Conclusion

   Congratulations! 🎉 You’ve successfully installed and initialized the Okto SDK in your React app. You’re now ready to start building with Okto's powerful features.

For more information or advanced configurations, refer to the [Okto Documentation](https://docs.okto.tech/docs)