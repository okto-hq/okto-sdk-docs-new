version: &imageVersion "13153602241"
global:
  version: *imageVersion
okto-sdk-docs-new-staging:
  environment: staging
  project: okto
  namespaceOverride: oktohq
  image:
    registry: ghcr.io
    repository: okto-hq/okto-sdk-docs-new
    pullPolicy: IfNotPresent
    tag: *imageVersion
  imagePullSecrets:
    - name: oktohq-registry
  deployment:
    resources:
      limits:
        cpu: 500m
        memory: 500Mi
      requests:
        cpu: 200m
        memory: 200Mi
    readinessProbe:
      failureThreshold: 10
      httpGet:
        path: /
        port: 3000
        scheme: HTTP
      initialDelaySeconds: 3
      periodSeconds: 2
    DDService: okto-sdk-docs-staging
    topologySpreadConstraints:
      - maxSkew: 1
        topologyKey: node
        whenUnsatisfiable: ScheduleAnyway
  service:
    # serviceName: 
    ports:
      - port: 3000
        portName: http
        protocol: TCP
    type: ClusterIP
  ingress:
    enabled: true
    domain: okto.tech
    exposePort: http
    scheme: http
    hosts:
      - host: docs-staging.okto.tech
  autoscaling:
    enabled: true
    minReplicas: 1
    maxReplicas: 2
  envFromSecrets:
    - variable: val1
      secretId: &oktohq-sdk-docs oktohq/okto-sdk-docs-staging
      secretKey: val1
    - variable: NEXT_PUBLIC_GOOGLE_CLIENT_ID
      secretId: *oktohq-sdk-docs
      secretKey: NEXT_PUBLIC_GOOGLE_CLIENT_ID
  tolerations:
    - effect: NoSchedule
      key: application
      operator: Equal
      value: okto
    - effect: NoSchedule
      key: arch
      operator: Equal
      value: arm64
  nodeSelector:
    application: okto
    arch: arm64
  securityContext:
    allowPrivilegeEscalation: false
    privileged: false
    runAsGroup: 7777
    runAsUser: 7777
  deploymentStrategy:
    rollingUpdate:
      maxUnavailable: 50%
